node_modules/
.env
data/
*.db
*.db-journal
*.db-wal
*.db-shm
.pathcheck
backups/

# Local Claude Code settings — launch configs and per-machine preferences.
# Nothing the app needs to run lives here.
.claude/
